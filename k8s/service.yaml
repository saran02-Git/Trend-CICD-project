apiVersion: v1
kind: Service
metadata:
  name: trend-node-service
  labels:
    app: trend-node-app
spec:
  type: LoadBalancer
  selector:
    app: trend-node-app
  ports:
    - name: http
      port: 80
      targetPort: 4000